# Requirements for Hugging Face Spaces (app.py only)
# Python 3.11 recommended

# Gradio UI
gradio>=4.0.0

# PyTorch CPU-only (installed via Dockerfile with --index-url)
# torch
# torchvision

# TensorFlow CPU-only
tensorflow-cpu>=2.16.0

# CLIP dependencies (CLIP itself installed via Dockerfile from git)
ftfy
regex

# Data processing
numpy>=1.24.0,<2.0
Pillow>=9.0.0

# HuggingFace model download
huggingface_hub
