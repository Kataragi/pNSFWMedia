# Requirements for Hugging Face Spaces (app.py only)
# PyTorch and CLIP are installed separately in Dockerfile

gradio>=4.0.0
tensorflow-cpu>=2.16.0
ftfy
regex
numpy>=1.24.0
Pillow>=9.0.0
huggingface_hub
